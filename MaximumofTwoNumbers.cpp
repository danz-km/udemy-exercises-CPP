#include<iostream>
using namespace std;

void max(int x,int y)
{
    //write conditions here to print maximum of 2 numbers
    //simple but the program's purpose is to print out which variable has a larger value than the other 
    if (x>y)
        cout<<x;
    else
        cout<<y;
    
}
